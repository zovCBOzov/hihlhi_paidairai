using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class qergsert : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Print(HelloWorld)
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
